const Navbar = ()=>{
    return(
        <header>
            <img src={"/reunion.jpg"} alt={""} className={"header-img"}/>
            <h2 className={"header-title"}><strong className={"header-title-span"}>re</strong>union</h2>
        </header>
    )
}

export default Navbar;